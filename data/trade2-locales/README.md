# Trade2 locale snapshots

Generated by `pnpm data:refresh` from the official `/api/trade2/data/*`
endpoints. Commit refreshed snapshots with a release when the official data
changes. `zh-cn` is derived from the official Taiwan snapshot with the
extension's local traditional-to-simplified conversion.
